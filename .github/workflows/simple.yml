name: Shell commands

on: [push]

jobs:
  
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo-string
        run: echo "first actions step"
      - name: multiline-script
        run: |
          noder -v
          npm -v

